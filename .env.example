PORT=5000
NODE_ENV=development

# Database
# Use DB_HOST=postgres when running with Docker
# Use DB_HOST=localhost when running without Docker
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=knowledge_hub
DB_EXPOSE_PORT=5433
