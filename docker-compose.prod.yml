services:
  server:
    build:
      context: .
      target: production
    container_name: knowledge-hub-server
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    env_file:
      - .env
    restart: unless-stopped
