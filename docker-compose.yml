services:
  server:
    build:
      context: .
      target: development
    container_name: knowledge-hub-server
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    restart: unless-stopped
